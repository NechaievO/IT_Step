.grid-container {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-gap: 10px;
    padding: 10px;
    border: 2px solid #ccc;
    gap: 30px;
}

.grid-item {
    background-color: #005f6b;
    color: white;
    padding: 20px;
    font-size: 1.5em;
    text-align: center;
}